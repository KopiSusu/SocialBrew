import * as types from "./../../actions";

export default (state = {
  	isFetching: false,
  	message: ""
}, action) => {
  	switch (action.type) {
  		default:
      		return state
      		break;
  	}
};

